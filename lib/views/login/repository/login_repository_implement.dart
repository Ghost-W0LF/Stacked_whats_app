import 'package:stacked_app/views/login/repository/login_repository.dart';

class LoginRepositoryImplement implements LoginRepository {
  @override
  Future<String?> firebaseLoginAuth(String email, String password) async {
    return null;
  }
}
