import 'package:flutter_test/flutter_test.dart';
import 'package:stacked_app/app/app.locator.dart';

void main() {
  group('GetRecivedChatServiceTest -', () {
    tearDown(() => locator.reset());
  });
}
